import styled from 'styled-components';

const Container = styled.div`
  padding: 0 clamp(1.75rem, 5vw + 1rem, 10rem);
`;

const Wrapper = styled.div`
  padding: 1.75rem 0;
`;

export { Container, Wrapper };
